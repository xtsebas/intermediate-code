// Test de inicializaciones por defecto

// Constante con inicializador explícito
const PI: integer = 314;

// Variables con inicializadores
let greeting: string = "Hello, Compiscript!";
let count: integer = 42;

// Variables sin inicializadores (deben generar tripletos con valores por defecto)
let flag: boolean;
let name: string;
let value: integer;

// Uso de las variables
print flag;
print name;
print value;
